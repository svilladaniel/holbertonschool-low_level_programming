#include <stdio.h>

/**
 * main - prints a to z
 * @void: Doest mean anything
* Description: prints a to z
* Return: 0
*/
int main(void)
{
	char c;

	for (c = 'a' ; c <= 'z' ; ++c)
		putchar(c);
	return (0);
}
