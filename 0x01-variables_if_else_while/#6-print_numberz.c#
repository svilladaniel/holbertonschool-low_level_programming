#include <stdio.h>

/**
 * main - prints 0 - 10, followed by a new line
 * @void: it doesnt mean anything
 * Description:prints 0 - 10, followed by a new line
 * Return: Description of the returned value
 */
int main(void)
{
	int a;

	for (a = 0 ; a < 10 ; a++)
		putchar(a + '0');
	putchar('\n');
	return (0);
}
