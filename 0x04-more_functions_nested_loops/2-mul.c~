#include "holberton.h"

/**
 * mul - multiplies two integers
 * @a: is an integer
 * @b: is an integer 
 * Description:  multiplies two integers
 * Return: c is the result of the math operation
 */

int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
