#include <stdio.h>

/**
 * int - prints alphabet a...zA...Z
 * @void: doesnt mean anything
 * Description: prints alphabet a...zA...Z
 * Return: 0
 */
int main(void)
{
	char a;
	char b;

	for (a = 'a' ; a <= 'z' ; ++a)
	{
		for (b = 'A' ; b <= 'Z' ; ++b)
		{
			putchar(a);
			putchar(b);
			putchar(\n);
		}
	}
	return (0);
}
