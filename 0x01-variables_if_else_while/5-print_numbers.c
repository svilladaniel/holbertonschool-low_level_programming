#include <stdio.h>

/**
 * main - prints digit from 0 to 9
 * @void:doesnt mean anything
 * Description: prints digits from 0 to 9
 * Return: 0
 */
int main(void)
{
	int a;

	for (a = 0 ; a < 10 ; a++)
		printf("%d", a);
	printf("\n");
	return (0);
}
