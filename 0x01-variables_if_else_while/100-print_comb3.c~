#include <stdio.h>

/**
 * main - prints the numbers from 00 to 89
 * @void: it doesnt mean anything
 * Description: prints the numbers from 00 to 89
 * Return: 0
 */
int main(void)
{
	int a;

	for (a = 0 ; a < 90 ; a++)
	{
		if (a % 10 > a / 10)
		{
			putchar((a / 10) + '0');
			putchar((a % 10) + '0');
		}
		if (a != 89)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
